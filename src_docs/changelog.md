# Changelog

## 2026-04-01 — Job Folder System (Issue #617)

- **feat**: `style-transfer` gains `--force` and `--cleanup` flags for job folder control.
- **feat**: `style-video` gains `--force` and `--cleanup` flags for job folder control.
- **feat**: All style exports now create a persistent job folder alongside the output, storing every intermediate artifact (`.lines`, `.svg`, raster). Interrupted jobs resume automatically.

## Previous releases

See [GitHub releases](https://github.com/vexyart/vexy-lines-cli/releases) for earlier versions.
